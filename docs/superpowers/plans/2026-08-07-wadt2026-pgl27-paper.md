# WADT 2026 PGL(2,7) Paper Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a complete, self-contained first draft of the WADT 2026 full paper whose prose and claims follow the approved design spec.

**Architecture:** Keep the first draft in one LaTeX source file so one writer controls the paper's voice and cross-section terminology. Put bibliography data in one local BibTeX file and build patterns in one local ignore file. A single academic-writing subagent owns the new paper directory and performs every write. The parent agent reads the completed source, checks it against the design and Rocq statements, and sends concrete corrections back to the same worker.

**Tech Stack:** LNCS LaTeX, BibTeX with `splncs04`, TikZ for an inline model diagram, local Rocq sources as read-only evidence, `latexmk` 4.87, TeX Live 2026.

## Global Constraints

- The approved design is `docs/superpowers/specs/2026-08-07-wadt2026-pgl27-paper-design.md` at commit `afd0e2b`.
- The writer owns only `pgg-smc/paper-wadt2026/main.tex`, `pgg-smc/paper-wadt2026/references.bib`, and `pgg-smc/paper-wadt2026/.gitignore`.
- The writer must not edit any `.v` file, project audit file, existing paper, design spec, or plan.
- The parent agent performs no writes in `pgg-smc/paper-wadt2026/`. It verifies by reading and returns corrections to the writer.
- The paper uses a monolithic `main.tex` for this draft. It does not create `sections/*.tex` yet.
- Use the TeX-distribution copies of `llncs.cls` and `splncs04.bst`. Do not copy an older class or style file.
- Do not use `minted`, `-shell-escape`, parent-directory macro imports, or parent-directory figure imports.
- Use an anonymized build switch. The default source metadata names Cheng-Hui Weng, Nagoya University, but the anonymous branch prints `Anonymous` until the chairs confirm the review policy.
- Keep the twenty-page content allocation. Do not implement a fifteen-page fallback or reduce technical content.
- Use `~/Projects/aplas2024-poster/forteApr22/forteApr22.tex` as the vocabulary and grammar baseline.
- Put the grammatical subject and main verb near the start of each sentence. Give one main idea to each sentence and one job to each paragraph.
- Preserve necessary mathematical terms. Simplify the grammar around them.
- Do not use em dashes, semicolons, prose asides in parentheses, generic openings, unsupported significance claims, decorative three-part lists, or repeated summary sentences.
- Use personal "I" for the solo author's choices and responsibility. Use mathematical "we" only for reader-inclusive constructions and proofs.
- Use mathematical concept names in body prose. Put Rocq identifiers in `\coqin{}` footnotes, tables, or source notes.
- The LLM workflow is methodology and disclosure only. It is not a research contribution and carries no priority claim.
- The main exact execution law is `pgl27P`: uniform Boolean prior, fixed `orbit_encode` representative, and independent uniform PGL(2,7) shuffle.
- State the recovery ramp as `(t,r,n)=(3,7,8)`. The `ThresholdScheme` record has no recovery-threshold field and its decoder reads all eight endpoints.
- State the orbit split as 28 equianharmonic subsets and 42 harmonic subsets. Never write `14+56`.
- State `var_dist` as unhalved L1 distance. The PGL bound is L1 at most `2^-40`, equivalent to halved total variation at most `2^-41`.
- `pgl27_word_mixing` is one certificate. `pgl27_endpoint_mixing` and `pgl27_joint_mixing` are transfers of that bound.
- Describe `pgl27_joint_mixing` as the product of any secret prior with the shuffle law. It is not an execution law.
- Do not describe `2^-40` as approximate trace secrecy or approximate coalition privacy.
- State that one specific four-card coalition leaks. Do not claim that every four-card coalition leaks.
- State the model limits in the abstract, introduction, and model section: passive adversaries, verifier learns the secret, no active security, no post-reveal security, and no composition.
- Use descriptive novelty wording until a separate literature audit licenses a priority claim.
- Keep the Barrington and NC1 compilation direction as continuity with the accepted abstract and as future work, not as a result of this paper.
- Treat the two-model security bridge as a separate future paper. This draft stops at shuffle-law, endpoint, and secret-prior product-law proximity.

---

## File Map

### Files to create

- `pgg-smc/paper-wadt2026/main.tex`
  - Complete paper source.
  - Preamble and anonymous switch.
  - Abstract and ten numbered sections.
  - Inline theorem statements, tables, one TikZ model diagram, acknowledgements, AI-use statement, disclosure, and artifact statement.
- `pgg-smc/paper-wadt2026/references.bib`
  - Local copies of the bibliography entries actually cited by `main.tex`.
  - Entries begin from `~/Projects/aplas2024-poster/wadtSep17/references.bib`.
  - The New Generation Computing metadata for Koch, Schrempp, and Kirsten is not used until publisher metadata is checked. Use the verified ASIACRYPT 2019 entry in this draft.
- `pgg-smc/paper-wadt2026/.gitignore`
  - Ignores LaTeX build products while preserving the source and bibliography.

### Files to read without editing

- `docs/superpowers/specs/2026-08-07-wadt2026-pgl27-paper-design.md`
- `docs/superpowers/notes/20260713-pgl27-claim-boundary.md`
- `pgg-smc/instances/pgl27/pgl27_orbit.v`
- `pgg-smc/instances/pgl27/pgl27_recovery.v`
- `pgg-smc/instances/pgl27/pgl27_secrecy.v`
- `pgg-smc/instances/pgl27/pgl27_trace.v`
- `pgg-smc/instances/pgl27/pgl27_mixing.v`
- `pgg-smc/instances/kim2025/five_card_kim.v`
- `~/Projects/aplas2024-poster/wadtSep17/main.tex`
- `~/Projects/aplas2024-poster/wadtSep17/references.bib`
- `~/Projects/aplas2024-poster/forteApr22/forteApr22.tex`

---

### Task 1: Create the self-contained LNCS source and bibliography

**Files:**
- Create: `pgg-smc/paper-wadt2026/main.tex`
- Create: `pgg-smc/paper-wadt2026/references.bib`
- Create: `pgg-smc/paper-wadt2026/.gitignore`

**Interfaces:**
- Consumes: TeX Live's installed `llncs.cls` and `splncs04.bst`.
- Produces: A compilable LNCS document with stable macros and all ten section labels used by later tasks.

- [ ] **Step 1: Confirm the destination is new**

Run:

```bash
test ! -e pgg-smc/paper-wadt2026/main.tex
test ! -e pgg-smc/paper-wadt2026/references.bib
test ! -e pgg-smc/paper-wadt2026/.gitignore
```

Expected: both commands exit 0.

- [ ] **Step 2: Create the preamble and complete section skeleton**

Create `main.tex` with this structure and no placeholder text:

```latex
\documentclass[runningheads]{llncs}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,mathtools}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{microtype}
\usepackage{xcolor}
\newif\ifanonymous
\anonymoustrue
\newcommand{\Rocq}{Rocq}
\newcommand{\coqin}[1]{\texttt{#1}}
\newcommand{\PG}{\mathrm{PGL}(2,7)}
\newcommand{\Unif}{U_G}
\newcommand{\wordlaw}{\mu^{*L}}
\title{Algebraic Specification of Card-Based Cryptographic Protocols in Rocq}
\ifanonymous
  \author{Anonymous}
  \institute{Submission to WADT 2026}
\else
  \author{Cheng-Hui Weng}
  \institute{Nagoya University}
\fi
\begin{document}
\maketitle
\section{Introduction}\label{sec:introduction}
\section{Protocol and Security Model}\label{sec:model}
\section{Framework and Generic Theorems}\label{sec:framework}
\section{The \texorpdfstring{$\PG$}{PGL(2,7)} Construction}\label{sec:pgl}
\section{Correctness, Recovery, and Exact-Shuffle Privacy}\label{sec:exact}
\section{Finite-Step Approximation of the Exact Model}\label{sec:mixing}
\section{Other Instances}\label{sec:instances}
\section{Methodology and Artifact}\label{sec:method}
\section{Related Work}\label{sec:related}
\section{Conclusion and Future Work}\label{sec:conclusion}
\subsubsection*{Acknowledgements and AI-use statement.}
\subsubsection*{Disclosure of Interests.}
\subsubsection*{Artifact availability.}
\bibliographystyle{splncs04}
\bibliography{references}
\end{document}
```

- [ ] **Step 3: Create the local bibliography**

Copy only cited entries from the accepted abstract's `references.bib`. The initial set is:

```text
denBoer1989
KimCetinkaya2025
MizukiSone2009
MizukiSone2012
Koch2019
DvorakKoucky2021
KochFUN2021
ShinagawaNuida2021
ShinagawaNuida2025
KochWalzerHartel2015
Barrington1989
WengEtAl2025
MathComp
CryptHOL
SSProve
CertiCrypt
LevinPeresWilmer2009
InfoTheo
```

Do not copy `Koch2020` in this draft. Keep the real authors in `WengEtAl2025`.

For the related-work axes, use the research knowledge base to verify and add
entries for these exact sources before citing them:

```text
Shinagawa and Miyamoto on graph-automorphism shuffles, arXiv:2109.00397
Shinagawa and Miyamoto on graph-automorphism shuffles, arXiv:2205.04774
Karayel, Expander Graphs, Isabelle AFP, 2023
Barthe et al., pRHL or xpRHL, POPL 2017
the 2025 four-card XOR APKC successor identified in the design audit
```

Invoke the repository's research workflow for the full-text check. Record the
publisher or archive URL, title, authors, venue, year, and exact supporting
section in the research knowledge base. Add a BibTeX entry only after those
fields and the attached paper claim have been checked.

- [ ] **Step 4: Create the build-product ignore list**

Create `.gitignore` with:

```gitignore
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.pdf
*.synctex.gz
```

- [ ] **Step 5: Run the skeleton build**

Run:

```bash
cd pgg-smc/paper-wadt2026
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
```

Expected: exit 0 and `main.pdf` exists. Citation warnings are allowed until later tasks cite the local entries. LaTeX errors are not allowed.

- [ ] **Step 6: Commit the scaffold**

```bash
git add pgg-smc/paper-wadt2026/main.tex pgg-smc/paper-wadt2026/references.bib pgg-smc/paper-wadt2026/.gitignore
git commit -m "wadt2026: scaffold full paper source"
```

---

### Task 2: Write the abstract and introduction

**Files:**
- Modify: `pgg-smc/paper-wadt2026/main.tex`

**Interfaces:**
- Consumes: the approved three contributions, accepted abstract continuity, and model limits.
- Produces: the terminology and contribution wording used by every later section.

- [ ] **Step 1: Insert a complete abstract after `\maketitle`**

Write 150 to 220 words. It must contain these facts in this order:

1. Card-based protocols use physical cards for information-theoretic computation.
2. Existing proofs and verification methods do not provide this paper's combination of reusable group-action structure, executed-trace privacy, and checked finite-step mixing.
3. The framework treats the finite group, permutation action, and shuffle law as first-class parameters.
4. The PGL instance has exact correctness, `(3,7,8)` recovery, and coalition trace privacy for at most three players under the main exact law.
5. A five-letter, 200-step word law is within unhalved L1 distance `2^-40` of uniform.
6. The finite-step theorem does not establish approximate trace privacy.
7. Four sibling instances show the scope of the framework.

Use descriptive novelty wording. Do not write "first".

- [ ] **Step 2: Write the introduction in six short paragraphs**

The six paragraph jobs are:

1. Define card-based cryptography and its information-theoretic goal.
2. Explain the verification gap using Koch, Schrempp, and Kirsten as the bounded-verification neighbor.
3. Connect the accepted abstract's den Boer and S5 account to the later PGL case without implying that PGL appeared in the workshop abstract.
4. State the three contributions in two or three bullets. Do not make LLM use a contribution.
5. State the exact and finite-step models and the missing security bridge.
6. Give the paper organization.

- [ ] **Step 3: Run the introduction AI-ism gate**

Run:

```bash
rg -n "—|;|Moreover|Furthermore|Consequently|It is worth noting|clearly|robust|comprehensive|novel|groundbreaking|This work|This paper" pgg-smc/paper-wadt2026/main.tex
```

Expected: every match is inspected. Remove formulaic uses. Keep only mathematically necessary uses with a clear local reason.

- [ ] **Step 4: Build and commit**

```bash
cd pgg-smc/paper-wadt2026
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
cd ../..
git add pgg-smc/paper-wadt2026/main.tex
git commit -m "wadt2026: draft abstract and introduction"
```

Expected: build exits 0. The abstract has no placeholder comment.

---

### Task 3: Write the model and framework sections

**Files:**
- Modify: `pgg-smc/paper-wadt2026/main.tex`

**Interfaces:**
- Consumes: `pgg_interface`, `ThresholdScheme`, transitivity privacy, trace secrecy, and weighted-word vocabulary.
- Produces: definitions of the exact law, finite-step law, view, trace, dealer law, and evidence boundaries used in Sections 4 to 6.

- [ ] **Step 1: Write Section 2 with explicit domains and priors**

Section 2 must define in prose and displays:

```latex
(G,\rho,\mu;R), \qquad U_G, \qquad \mu^{*L},
```

and explain:

- the secret, dealt arrangement, player view, coalition view, and executed trace
- `pgl27P` as uniform Boolean prior, fixed representative, and independent uniform group element
- `pgl27P_alldecks` as a robustness model with a uniform valid deck from the chosen class
- passive adversaries
- the verifier's intended knowledge
- no active security, post-reveal security, or composition
- unhalved L1 distance and the relation to halved total variation

- [ ] **Step 2: Add one inline TikZ diagram of the two model paths**

The diagram must show:

```text
uniform group law U_G -> exact view and trace theorems
five-letter word law mu^*200 -> L1 mixing -> endpoint and product-law transfers
```

Draw no arrow from finite-step mixing to approximate view or trace privacy. Mark that bridge as future work in the caption.

- [ ] **Step 3: Write Section 3 around used framework components only**

Explain the role of:

- the group and representation interface
- the sharing scheme and full decoder
- the interpreter and executed traces
- the transitivity-to-view-independence theorem
- the view-to-trace secrecy theorem
- the weighted-word law

Describe `MonodromyProfile`, `SecurityWitness`, and `ReconPlug` in one short paragraph as packaging. Do not credit unused covering, genus, Klein, dropout, asymptotic, or algebraic-geometry records.

- [ ] **Step 4: Run the model and framework claim check**

Run:

```bash
rg -n "active security|composition|post-reveal|uniform Boolean|pgl27P|L1|total variation|SecurityWitness|ReconPlug" pgg-smc/paper-wadt2026/main.tex
```

Expected: every required term occurs in the intended section and carries the approved qualification.

- [ ] **Step 5: Build and commit**

```bash
cd pgg-smc/paper-wadt2026
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
cd ../..
git add pgg-smc/paper-wadt2026/main.tex
git commit -m "wadt2026: draft model and framework"
```

---

### Task 4: Write the PGL construction and exact-security sections

**Files:**
- Modify: `pgg-smc/paper-wadt2026/main.tex`

**Interfaces:**
- Consumes: PGL group, orbit, recovery, secrecy, and trace theorem statements.
- Produces: the paper's main mathematical construction and exact-model theorem chain.

- [ ] **Step 1: Write Section 4 from the PGL source statements**

State:

- the action of PGL(2,7) on eight projective points
- the group order 336
- the orbit-class Boolean secret on four-subsets
- 28 equianharmonic and 42 harmonic subsets
- 3-transitivity as the source of privacy for coalitions of size at most three

Use theorem environments with mathematical statements. Put supporting Rocq names in footnotes or a compact source table. Include `pgl27_3transitive`, `orbit_class_split`, `orbit_class_split_complement`, and `orbit_encodeK`.

- [ ] **Step 2: Write the recovery subsection of Section 5**

State the ramp as:

```text
private through 3, some leakage at 4, ambiguous through 6, determined at 7, implemented decoder reads 8
```

Say that one fixed four-card coalition witnesses sharpness. Cite `pgl27_view_dep_k4` and `pgl27_view_leak_k4`. Use `pgl27_reveal_ambiguous` for the every-six-set statement and `pgl27_seven_reveal_class` for determination.

- [ ] **Step 3: Write the exact view and trace privacy subsection**

State each result with its secret prior and dealer law. Separate:

- fixed representative with uniform group shuffle
- all-decks dealer with uniform secret prior
- shuffle-free deck view independence with arbitrary prior

Do not state a prior-generic trace theorem. State that the boolp trio is the trust base for probability and trace results.

- [ ] **Step 4: Run the exact-claim guard**

Run:

```bash
rg -n "14\+56|every four|arbitrary prior.*trace|trace.*arbitrary prior|decoder.*seven" pgg-smc/paper-wadt2026/main.tex
```

Expected: no matches that assert a forbidden claim.

- [ ] **Step 5: Build and commit**

```bash
cd pgg-smc/paper-wadt2026
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
cd ../..
git add pgg-smc/paper-wadt2026/main.tex
git commit -m "wadt2026: draft PGL construction and exact security"
```

---

### Task 5: Write the finite-step approximation section

**Files:**
- Modify: `pgg-smc/paper-wadt2026/main.tex`

**Interfaces:**
- Consumes: `pgl27_word_mixing`, `pgl27_endpoint_mixing`, and `pgl27_joint_mixing`.
- Produces: the exact relation between the realistic word law and the uniform law.

- [ ] **Step 1: Describe the certificate inputs**

State that the alphabet contains five letters from three generators and two added inverses. State the word count `5^200`, group order 336, and the fiber-counting certificate.

- [ ] **Step 2: State one main bound and two transfers**

Use one main theorem display:

```latex
\left\lVert \mu^{*200}-U_G\right\rVert_1 \le 2^{-40}.
```

Then state:

- every single-card marginal has the same upper bound by data processing
- the product of any secret prior with each shuffle law has the same upper bound
- under the halved convention, total variation is at most `2^-41`

Do not call the product law an execution law. Do not count the transfers as independent certificates.

- [ ] **Step 3: End with the missing bridge**

State positively what is proved, then state the limit in one short paragraph. The finite-step results stop before approximate coalition view privacy and approximate executed-trace secrecy.

- [ ] **Step 4: Run the finite-step guard**

Run:

```bash
rg -n "2\^\{-40\}|2\^-40|trace secrecy|coalition privacy|independent certificate|joint.*execution" pgg-smc/paper-wadt2026/main.tex
```

Expected: inspect every bound occurrence. No sentence may attach `2^-40` to trace secrecy or coalition privacy.

- [ ] **Step 5: Build and commit**

```bash
cd pgg-smc/paper-wadt2026
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
cd ../..
git add pgg-smc/paper-wadt2026/main.tex
git commit -m "wadt2026: draft finite-step approximation"
```

---

### Task 6: Write comparison, methodology, related work, and conclusion

**Files:**
- Modify: `pgg-smc/paper-wadt2026/main.tex`

**Interfaces:**
- Consumes: the four sibling instances, accepted abstract, local bibliography, and approved disclosure policy.
- Produces: the paper's breadth, reproducibility account, literature position, and future-work boundary.

- [ ] **Step 1: Write the cross-instance comparison table in Section 7**

Rows are den Boer, Kim, S5, S5xS5, and PGL(2,7). Columns are recovery, exact privacy, trace privacy, finite-step mixing, and trust base.

The table must record:

- den Boer exact one-step behavior
- Kim's in-kernel `L = 7` result
- S5 and S5xS5 assumed-certificate routes
- PGL's `L = 200` certificate and full exact security stack

- [ ] **Step 2: Write Section 8 without presenting LLM use as a contribution**

Cover:

- fixed commit and version reporting
- `make -j1` for Rocq reproduction
- paper-specific theorem index and claim matrix
- per-theorem `Print Assumptions`
- excluded axioms and the out-of-build `Admitted` file
- LLM-generated proof scripts and documentation drafts under human specification, review, and responsibility

Do not invent token, wall-clock, file-count, or theorem-count values. If a number has not been regenerated, omit the number and describe the required report.

- [ ] **Step 3: Write Section 9 around four evidence-backed axes**

Use these axes:

1. Card-protocol verification
2. Group-parametric shuffle models
3. Machine-checked mixing
4. Proof-assistant cryptography and trace security

Use only entries whose full text and metadata were checked in Task 1. Include
the graph-automorphism shuffle papers on the group-parametric axis, Karayel and
Barthe et al. on the machine-checked mixing axis, and the four-card XOR paper
on the card-verification axis. Do not use a priority claim. Describe the FORTE
2025 work as the foundation of the trace interpreter.

- [ ] **Step 4: Write Section 10 and disclosures**

The conclusion states the exact results, finite-step relation, and model limits once. Future work names:

- the general bridge from finite-step mixing to approximate view and trace privacy as a separate two-model paper
- the Barrington and NC1 compilation direction from the workshop abstract
- active and compositional security
- quantitative leakage for four through six cards

The AI-use statement uses the exact methodology wording from the spec. The disclosure states either no competing interests or the author's actual disclosure. Do not invent funding.

- [ ] **Step 5: Build and commit**

```bash
cd pgg-smc/paper-wadt2026
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
cd ../..
git add pgg-smc/paper-wadt2026/main.tex pgg-smc/paper-wadt2026/references.bib
git commit -m "wadt2026: complete full paper draft"
```

---

### Task 7: Run the worker's complete prose and source audit

**Files:**
- Modify: `pgg-smc/paper-wadt2026/main.tex`
- Modify only if needed: `pgg-smc/paper-wadt2026/references.bib`

**Interfaces:**
- Consumes: the complete draft.
- Produces: a draft ready for the parent agent's read-only review.

- [ ] **Step 1: Remove all drafting placeholders**

Run:

```bash
rg -n "[T]ODO|[T]BD|[F]IXME|[P]LACEHOLDER|[X]XX|write later|citation needed" pgg-smc/paper-wadt2026/main.tex pgg-smc/paper-wadt2026/references.bib
```

Expected: no matches.

- [ ] **Step 2: Run the hard style scan**

Run:

```bash
rg -n "—|;|Moreover|Furthermore|Consequently|It is worth noting|delve|pivotal|crucial|groundbreaking|comprehensive|robust|seamless|This work|This paper" pgg-smc/paper-wadt2026/main.tex
```

Expected: inspect each match and remove formulaic uses. Do not alter semicolons that occur inside a mathematical tuple or BibTeX field solely to satisfy the prose rule.

- [ ] **Step 3: Run the claim scan**

```bash
rg -n "14\+56|first refereed|proof authorship|agents as authors|approximate trace|approximate coalition|every four|decoder.*seven|standard total variation.*2\^\{-40\}" pgg-smc/paper-wadt2026/main.tex
```

Expected: only explicit statements that these claims are not established may match.

- [ ] **Step 4: Check citations and references**

Run:

```bash
cd pgg-smc/paper-wadt2026
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
rg -n "undefined citations|Citation.*undefined|There were undefined references" main.log
```

Expected: `latexmk` exits 0 and the log scan has no matches.

- [ ] **Step 5: Check that the writer changed only owned files**

Resolve the committed plan as the comparison base:

```bash
WADT_PLAN_BASE=$(git log -1 --format=%H -- docs/superpowers/plans/2026-08-07-wadt2026-pgl27-paper.md)
git diff --name-only "$WADT_PLAN_BASE"..HEAD
```

Expected tracked paper changes:

```text
pgg-smc/paper-wadt2026/main.tex
pgg-smc/paper-wadt2026/references.bib
pgg-smc/paper-wadt2026/.gitignore
```

The worker must not stage generated PDF, AUX, LOG, BBL, BLG, FDB, or FLS files.

- [ ] **Step 6: Commit audit fixes**

```bash
git add pgg-smc/paper-wadt2026/main.tex pgg-smc/paper-wadt2026/references.bib
git commit -m "wadt2026: pass prose and claim audit"
```

If the audit required no tracked changes, do not create an empty commit.

---

### Task 8: Parent read-only verification and correction loop

**Files:**
- Read: `pgg-smc/paper-wadt2026/main.tex`
- Read: `pgg-smc/paper-wadt2026/references.bib`
- Read: relevant Rocq theorem statements
- Worker modifies on request: `pgg-smc/paper-wadt2026/main.tex`

**Interfaces:**
- Consumes: the worker's completed paper.
- Produces: verified paper source or a line-specific correction request returned to the same worker.

- [ ] **Step 1: Parent reads the entire source in consecutive chunks**

Use `sed` ranges that cover line 1 through end of file. Do not rely on a summary or diff alone.

- [ ] **Step 2: Parent checks every mathematical paragraph against the named Rocq statement**

At minimum, verify the orbit split, ramp, fixed four-card witness, prior and dealer choices, exact trace claims, five-letter law, group order, one certificate plus two transfers, and missing bridge.

- [ ] **Step 3: Parent checks prose against the FORTE baseline**

Check sentence structure, term introduction, paragraph jobs, repeated transitions, summary repetition, unsupported evaluation, and code identifiers in body prose.

- [ ] **Step 4: Parent sends all concrete corrections to the same worker**

Each correction includes the file line, the current text, the exact defect, and the required claim or wording. The parent does not edit the paper source.

- [ ] **Step 5: Worker applies corrections and reruns Task 7**

The correction loop ends only when the parent can read the full source without finding a spec violation.

- [ ] **Step 6: Parent performs final repository verification**

Run:

```bash
WADT_PLAN_BASE=$(git log -1 --format=%H -- docs/superpowers/plans/2026-08-07-wadt2026-pgl27-paper.md)
git diff --check "$WADT_PLAN_BASE"..HEAD
git status --short
cd pgg-smc/paper-wadt2026
latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex
```

Expected: no whitespace errors, no unexpected tracked files, and a successful build. Existing unrelated untracked files remain untouched.
