id: S996
category: operational
title: "Stage 2 budget exhausted before all chunks ran (daily, token, or wall cap)"
severity: error
enabled: true
stage_mode: sentinel
authority:
  - audit-pipeline-2026-04
scope: changed_files
fix_hint: "Stage 2 could not complete. Either raise the triggered cap (ROCQ_AUDIT_TOKEN_CAP, ROCQ_AUDIT_WALL_SECONDS, or cfg.daily_token_cap), reduce the staged diff size, or acknowledge the gap with ROCQ_AUDIT_BYPASS=1. The commit is blocked until resolved."
exceptions: []
