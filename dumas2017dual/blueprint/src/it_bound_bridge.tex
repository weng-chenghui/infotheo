% =====================================================================
% Part II: Output secrecy and the full bound.
%
% Continues the Part I derivation across Alice's decrypt-receive to expose the
% scalar-product output S, builds the absolute-Pr guessing layer on the DERIVED
% endpoint game, transports the Infotheo fiber 1/m to that game by reflecting
% the experiment's samples and framing out the opaque predictor, and composes
% with the Part I 2*epsilon_cpa.
%
% Colour / status convention (same as content.tex):
%   \rocqok + \rocq{...}  green : EXISTS in the formalization, cited as a black box.
% The whole output-channel chain is committed and machine-checked; every node is
% green.  Chapter ref{ch:deriving_output}: the derivation extension and the
% S-exposing games + 2*epsilon_cpa.  Chapter ref{ch:guessing_experiment}: the
% guessing experiment and the connector.  Chapter ref{ch:it_bound}: the footprint
% frame, the sample reflection, the output-determined guess kernel and
% guess _|_ V2 | S, the route-F fiber bound, and the triangle composition.
%
% Statement bodies are terse mathematical statements (no proof, no status, no
% motivation).  Proof strategy and the exact Coq names live in "% proof:" /
% "% to create:" comments.  Identifiers are written in clean math (no \texttt).
%
% Module prefixes cited green:
%   infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_derivation
%   infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_code
%   infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_indcpa_advantage
%   infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber
%   infotheo.dumas2017dual.dsdp.counting.dsdp_entropy
%   infotheo.dumas2017dual.entropy_fiber.entropy_fiber_zpq
%
% New notation local to Part II.
\newcommand{\fib}{\mathrm{fiber}}
\newcommand{\guess}{\mathrm{guess}}
\newcommand{\cond}{\mathrm{cond}}
\newcommand{\Prexp}{\Pr\nolimits_{\mathrm{exp}}}
\newcommand{\jointfdist}{J}        % the (guess, V_2) joint fdist (margin of P)
% =====================================================================

\chapter{Absolute probability versus relational advantage}
\label{ch:absolute_pr}

\ssprove{} measures security relationally. Its headline quantity is the
\emph{advantage}, the difference between two games' success probabilities, and
its whole indistinguishability calculus bounds only such differences. The output
channel asks a different question: the standalone probability that one game lets
a predictor name Bob's secret $V_2$. That standalone quantity is an
\emph{absolute} probability, ``absolute'' marking the contrast with the relative
quantity the relational calculus returns. This chapter fixes the two notions,
records why the relational machinery cannot deliver the absolute one, and states
the two-leg composition that the contrast forces on Part~II.

\begin{definition}[Relational advantage]
  \label{def:relational_advantage}
  \rocqok
  % library: SSProve AdvantageE (Crypt.pkg_advantage); the difference bounded by
  %   eq_rel_perf_ind and the perfect-indistinguishability lemmas.  No \rocq link
  %   (library fact, outside the project's coqdoc), as for lem:exist_pr_code.
  The advantage of a distinguisher $A$ between two closed games is the absolute
  difference of the probabilities that $A$, composed with each game, returns
  true. \ssprove{}'s indistinguishability calculus bounds this difference and
  never the magnitude of either term alone.
  \[ \AdvE(G_0, G_1, A)
        = \bigl|\,\Pr[\,A\circ G_0 = \mathrm{true}\,]
                - \Pr[\,A\circ G_1 = \mathrm{true}\,]\,\bigr| \]
\end{definition}

\begin{definition}[Absolute success probability]
  \label{def:absolute_pr}
  \rocqok
  % library: SSProve Pr / Pr_code / Pr_fst (Crypt.nominal.Pr); the standalone
  %   true-mass of one closed game run from the empty heap.  Reflected as an
  %   iterated sum over the game's samples by lem:exist_pr_code.
  The absolute success probability of one closed game is the true-mass of its
  denotation run from the empty heap. It is a property of a single game, not a
  difference of two.
  \[ \Pr[\,G = \mathrm{true}\,]
        = \mu\bigl(\mathrm{Pr}_{\mathrm{code}}(G)(\emptyset)\bigr)(\mathrm{true}) \]
\end{definition}

\textbf{The obstruction.} Every relational rule rewrites or bounds a difference
$\AdvE(G_0, G_1, A)$; none yields the absolute $\Pr[\,G = \mathrm{true}\,]$ of a
lone game. The bound the fiber argument supplies is exactly such an absolute
statement, the success probability of the single guessing game
(Definition~\ref{def:guessing_experiment_leak_S}),
\[ \Prexp(\zero_S) \le 1/m, \]
so it lies outside the reach of the advantage calculus. The absolute leg is
therefore discharged through the denotational layer of
Definition~\ref{def:absolute_pr} directly: the connector
(Lemma~\ref{lem:guess_success_sdistr_eq_fdist}) reflects $\Prexp(\zero_S)$ as a
distribution probability, and the footprint frame
(Lemma~\ref{lem:Pr_fst_agree_locs}) carries the Infotheo $1/m$ across, never
forming an advantage.

\textbf{The two legs and their join.} The output channel splits into one
absolute leg and one relational leg, joined by the triangle inequality. The
absolute leg bounds the all-zero endpoint's standalone probability by $1/m$
(Theorem~\ref{thm:guess_sdistr_success_le}); the relational leg keeps
the real-to-zero distance inside the advantage world at $2\,\epscpa$
(Lemma~\ref{lem:guess_advantage_le}). One term is a single-game probability
and the other a difference of two, which is why the triangle inequality is what
joins them (Theorem~\ref{thm:dsdp_alice_secrecy_leak_S}):
\[ \underbrace{\Prexp(\real_S)}_{\text{absolute}}
   \;\le\;
   \underbrace{\Prexp(\zero_S)}_{\le\, 1/m\ \text{(absolute leg)}}
   \;+\;
   \underbrace{\bigl|\,\Prexp(\real_S) - \Prexp(\zero_S)\,\bigr|}
             _{\le\, 2\,\epscpa\ \text{(relational leg)}}
   \;\le\; 1/m + 2\,\epscpa. \]

\chapter{Extending the derivation to Alice's decryption}
\label{ch:deriving_output}

In the protocol Alice receives the masked aggregate, decrypts it, and removes
her masks to read the scalar-product output $S=U_1V_1+U_2V_2+U_3V_3$. The Part~I
walk stops one reception short of this decrypt
(Definition~\ref{def:walk_obs}), so the derived view exposes only the
ciphertexts. This chapter continues the same walk across that decrypt-reception,
lowers and denotes the new observation, and so the derived endpoint game exposes
$S$ alongside the ciphertexts. Adjoining $S$ to both endpoints is a common
deterministic post-processing: it adds no encryption hop, so the Part~I bound
$2\,\epscpa$ carries over unchanged.

\begin{definition}[Output reception]
  \label{def:alice_obs_output}
  \rocq{infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_derivation.AO_recv_output}
  \rocqok
  \uses{def:alice_obs}
  The corrupted-view vocabulary (Definition~\ref{def:alice_obs}) gains one
  action, $\mathsf{recvOut}$: receive the decrypted output and bind it to a
  result name. It is not an encryption hop.
  \[ o \mathrel{::=} \dots \mid \mathsf{recvOut}(x) \]
\end{definition}

\begin{definition}[Walking across the decrypt-reception]
  \label{def:walk_obs_output}
  \rocq{infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_derivation.walk_obs,
       infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_derivation.dsdp_received_responses_output}
  \rocqok
  \uses{def:walk_obs,def:alice_obs_output}
  At the decrypt-reception the extended walk reads the received plaintext off the
  three-element response stream and emits the output reception instead of
  halting. The ciphertext receptions and assemblies before it are unchanged, so
  the encryption-hop count is unchanged.
\end{definition}

\begin{lemma}[The derived output-exposing DSDP trace]
  \label{lem:obs_of_procs_dsdp_leak_S}
  \rocq{infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_derivation.obs_of_procs_dsdp_leak_S,
       infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_derivation.dsdp_obs_hops_leak_S}
  \rocqok
  \uses{lem:obs_of_procs_dsdp,def:walk_obs_output}
  For the DSDP inputs the extended trace computes to the Part~I trace
  (Lemma~\ref{lem:obs_of_procs_dsdp}) with the output reception inserted before
  the leak; its two encryption hops are exactly the Part~I two.
  \[ \tau^{S}_{\mathrm{DSDP}}
        = \tau_{\mathrm{DSDP}} \text{ with } \mathsf{recvOut}(S)
        \text{ before } \mathsf{leak} \]
\end{lemma}

\begin{definition}[Lowering the output reception]
  \label{def:lower_obs_output}
  \rocq{infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_derivation.lower_obs}
  \rocqok
  \uses{def:lower_obs,def:alice_obs_output}
  The lowering pass maps the output reception to the resolved output term written
  to a dedicated output cell by the dedicated output-write statement
  $\mathsf{put}_S$ (a distinct game-code constructor,
  Definition~\ref{def:embedding}). It is not an encryption hop, so the hop count
  is unchanged.
  \[ \mathsf{recvOut}(S) \;\mapsto\; \mathsf{put}_{S}(S) \]
\end{definition}

\begin{definition}[Denoting the output and exposing it]
  \label{def:denote_game_leak_S}
  \rocq{infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_code.Sout_cell,
       infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_code.id_Sout_get,
       infotheo.dumas2017dual.dsdp.symbolic_game.dsdp_game_code.denote_game_leak_S}
  \rocqok
  \uses{def:denote_game,def:lower_obs_output}
  Denotation writes the lowered output to a dedicated cell and adds one oracle
  reading it, leaving the return value and every other statement as in Part~I
  (Definition~\ref{def:denote_game}).
  \[ \sem{\mathsf{put}_{S}(S)} : \text{write } S \text{ to } S\text{-cell},
        \qquad \mathsf{getOut} : \mathbf{1} \to \mathsf{msg} \]
\end{definition}

\begin{definition}[The output-exposing endpoint games]
  \label{def:games_leak_s}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_indcpa_advantage.real_game_leak_S,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_indcpa_advantage.zero_game_leak_S}
  \rocqok
  \uses{def:denote_game_leak_S,def:games}
  The derived ciphertext games (Definition~\ref{def:games}) with the output write
  adjoined: each exposes the four ciphertexts and the scalar-product output $S$.
  The ciphertext structure is derived from the program; the output value is
  supplied directly in fiber form, $S = u_1 v_1 + u_2 v_2 + u_3 v_3$, the value
  Alice's decryption computes by the protocol's correctness. Adjoining $S$ is a
  common deterministic post-processing, adding no encryption hop. Write $\real_S$
  and $\zero_S$ for the two.
  \[ \view = \big(\,[\,a_1,a_2,c_2,c_3\,],\ S\,\big),\qquad
        S = u_1 v_1 + u_2 v_2 + u_3 v_3 \]
\end{definition}

\begin{lemma}[Computational distance, output-exposing games]
  \label{lem:dsdp_advantage_derived_leak_S}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_indcpa_advantage.dsdp_advantage_derived_leak_S}
  \rocqok
  \uses{def:games_leak_s,thm:dsdp_secure}
  The advantage between the output-exposing real and all-zero games is at most
  $2\,\epscpa$, the Part~I bound (Theorem~\ref{thm:dsdp_secure}). The $S$-oracle
  rides the hybrid ladder exactly as the $V_2$-oracle does.
  \[ \AdvE(\,\real_S,\ \zero_S,\ A\,) \le 2\,\epscpa \]
\end{lemma}

\chapter{The output-secrecy guessing experiment}
\label{ch:guessing_experiment}

The output channel is measured not by an advantage but by the absolute
probability that a predictor, reading the view including $S$, names Bob's secret
$V_2$. This chapter builds that guessing experiment on the derived endpoint game
and rewrites its success probability as an Infotheo distribution probability,
the single identity that carries the fiber bound of Chapter~\ref{ch:it_bound} to
the \ssprove{} side. The identity is read off the experiment as a pushforward:
the distribution of the $(\guess,V_2)$ pair the experiment produces is itself an
Infotheo distribution, and the success probability is the probability of its
diagonal.

\begin{definition}[Guessing experiment on the endpoint game]
  \label{def:guessing_experiment_leak_S}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.predictor_guesser,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guessing_challenger,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guessing_experiment}
  \rocqok
  \uses{def:games_leak_s,def:absolute_pr}
  A predictor reads the leaked ciphertexts and the output $S$ and returns a
  guess. The experiment runs it against the endpoint game and tests
  $\guess = V_2$. Its success probability is written $\Prexp(\text{game})$.
  \[ \Prexp(\text{game})
        = \mu\big(\mathrm{Pr}(\,\text{challenger}\circ
        (\text{predictor}\parallel\text{game})\,)\big)(\mathrm{true}) \]
\end{definition}

\begin{lemma}[\ssprove{} absolute-$\mathrm{Pr}$ reflection calculus (library)]
  \label{lem:exist_pr_code}
  \rocqok
  % library: Pr_code_ret/_sample/_bind/_get/_put, Pr_fst_sample/_bind, Pr_Pr_fst
  %   (Crypt.nominal.Pr), dlet_uniform (Crypt.HybridArgument), distr.dmargin*.
  SSProve's nominal layer reflects a closed program's probability as an iterated
  sum over its sample operations, a uniform draw of domain $n$ contributing the
  factor $1/n$, a cell read returning the value last written, and a bind
  reflecting compositionally even through an opaque continuation. Its
  subdistribution type is the mathcomp-analysis distribution
  $\{\mathrm{distr}\ A/R\}$.
  \[ \mathrm{Pr}(x \leftarrow \mathrm{sample}\ \mathrm{unif}\,n \,;;\, k\,x)
        = \tfrac1n{\textstyle\sum_x}\mathrm{Pr}(k\,x) \]
\end{lemma}

\begin{definition}[The guessing-experiment joint distribution]
  \label{def:guess_joint_fdist}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_joint_fdist,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_joint_code}
  \rocqok
  % renamed from dsdp_guess_fdist / dsdp_guess_core (prefix policy drops dsdp_
  %   on section-locals; guess_joint_* carries the (guess, V2) role).
  The Infotheo fdist of the $(\guess,V_2)$ pair the experiment produces: it is
  the pushforward $\mathrm{Pr}_{\mathrm{fst}}$ of the closed pair-returning code
  (the experiment without the equality test) into the finite carrier.
  \[ \jointfdist : \{\mathrm{fdist}\ (\guess,V_2)\}
        = \mathrm{Pr}_{\mathrm{fst}}(\text{pair code}) \]
\end{definition}

\begin{lemma}[The game probability is the joint diagonal]
  \label{lem:guess_success_sdistr_eq_fdist}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_sdistr_success,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_fdist_success,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_success_sdistr_eq_fdist}
  \rocqok
  % renamed from Pr_guess_enc_zero_leak_S_eqE, restructured to name both sides:
  %   guess_sdistr_success (SSProve subdistribution mass) = guess_fdist_success
  %   (Infotheo fdist diagonal mass), mirroring sdistr_to_fdist.
  \uses{def:games_leak_s,def:guess_joint_fdist,def:guessing_experiment_leak_S,lem:exist_pr_code}
  % proof (committed): the bool experiment is the pair code post-composed with
  %   the equality test; Pr_Pr_fst + the bind law (Pr_code_bind) factor the test
  %   through the pair code's distribution, which IS the joint fdist (predictor
  %   opaque throughout).  Carries one hypothesis: the predictor is lossless.
  $\Prexp(\zero_S)$ equals the probability, under the joint fdist
  (Definition~\ref{def:guess_joint_fdist}), of the diagonal event
  $\guess = V_2$. The bool experiment is the pair code followed by the equality
  test, so its success probability is the diagonal mass of the pair code's
  distribution; the predictor is never inspected.
  \[ \Prexp(\zero_S) = \Pr\nolimits_{\jointfdist}[\,\guess = V_2\,] \]
\end{lemma}

\chapter{The information-theoretic bound and full secrecy}
\label{ch:it_bound}

Conditioned on the leaked output $S$, Bob's secret $V_2$ is uniform over a fiber
of $m$ points, so any guess succeeds with probability at most $1/m$. The
predictor reads only the view and $S$, so its guess is conditionally independent
of $V_2$ given $S$; with the fiber bound this caps the diagonal mass of the
$(\guess,V_2)$ joint. This chapter reflects the experiment's samples into an
explicit distribution, proves that conditional independence through an
output-determined guess kernel, instantiates the route-F fiber bound, and pushes
the resulting $1/m$ through the connector and the computational distance to the
full bound $1/m + 2\,\epscpa$.

\section{Reused Infotheo facts (route F)}

\begin{lemma}[Fiber cardinality is $m$]
  \label{lem:exist_fiber_card}
  \rocq{infotheo.dumas2017dual.dsdp.counting.dsdp_entropy.dsdp_fiber_card_ring}
  \rocqok
  Over a commutative ring, when the multiplier $u_3$ is injective the solution
  set of the scalar-product constraint has exactly $\#R = m$ points, the image of
  the ring under $v \mapsto u_3 v$.
  \[ \#\lvert \fib(u_1,u_2,u_3,v_1,s)\rvert = \#R = m \]
\end{lemma}

\begin{lemma}[Conditional uniformity on the fiber]
  \label{lem:exist_cpr_fiber}
  \rocq{infotheo.dumas2017dual.entropy_fiber.entropy_fiber_zpq.gen_cPr_uniform_fiber}
  \rocqok
  \uses{lem:exist_fiber_card}
  For a joint distribution in which the variable pair is uniform and
  independent of the inputs, conditioning on the revealed values leaves the
  variable uniform over its fiber.
  \[ \Pr[\,\mathrm{Var}=v \mid \mathrm{Cond}=\cond\,]
        = \#\lvert\fib(\cond)\rvert^{-1} \]
\end{lemma}

\begin{lemma}[DSDP solution is uniform, probability $1/m$]
  \label{lem:exist_pr_sol}
  \rocq{infotheo.dumas2017dual.dsdp.counting.dsdp_entropy.Pr_dsdp_sol_uniform_ring}
  \rocqok
  \uses{lem:exist_cpr_fiber,lem:exist_fiber_card}
  Over a commutative ring with injective multiplier, each solution $(v_2,v_3)$ of
  the DSDP constraint has conditional probability $1/m$ given the inputs and $S$.
  \[ \Pr[\,(V_2,V_3)=(v_2,v_3)\mid \mathrm{Cond}=\cond\,] = m^{-1} \]
\end{lemma}

\section{Reflecting the experiment and framing the predictor}

\begin{lemma}[Footprint of valid code]
  \label{lem:Pr_fst_agree_locs}
  \rocq{infotheo.dumas2017dual.dsdp.convert.dsdp_convert.Pr_fst_agree_locs,
       infotheo.dumas2017dual.dsdp.convert.dsdp_convert.Pr_fst_closed}
  \rocqok
  \uses{lem:exist_pr_code}
  % proof: induction on valid_code; the get/put cases touch only L, the op case
  %   is vacuous for the empty import interface, sample/ret thread the heap.
  %   Pr_fst_closed is the L = emptym corollary.
  The value-marginal of valid code depends only on the heap restricted to the
  code's own locations $L$: heaps agreeing on $L$ give equal
  $\mathrm{Pr}_{\mathrm{fst}}$. This separation-logic footprint --- a program
  observes only the locations it reads or writes --- makes the predictor, whose
  $L$ excludes the secret cell, depend on $V_2$ only through what its inputs
  reveal.
  \[ h =_{L} h' \;\Longrightarrow\;
        \mathrm{dmargin}_{\mathrm{fst}}(\mathrm{Pr}_{\mathrm{code}}\,c\,h)
        = \mathrm{dmargin}_{\mathrm{fst}}(\mathrm{Pr}_{\mathrm{code}}\,c\,h') \]
\end{lemma}

\begin{definition}[The experiment's sample distribution]
  \label{def:guess_sample_fdist}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_sample_fdist,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_rv,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.Sout}
  \rocqok
  \uses{def:guessing_experiment_leak_S}
  % impl: guess_sample_fdist := sdistr_to_fdist of the lossless guessing code; the
  %   RVs guess_rv, V2, V3, Sout (and the constant inputs V1,U1,U2,U3) over it.
  The Infotheo fdist of the zero-game's samples: the secret pair $(V_2,V_3)$ and
  the masks are uniform and independent, $S$ is their constraint function
  $\mathrm{Sout}$, and $\guess$ follows the predictor's output on $(\view,S)$ ---
  a uniform product on the secrets composed with the predictor kernel.
  \[ P : \{\mathrm{fdist}\ \mathrm{Samples}\},
        \qquad V_2,V_3,S,\guess : \{\mathrm{RV}\ P\} \]
\end{definition}

\begin{lemma}[The joint distribution is the sample distribution's image]
  \label{lem:guess_joint_fdist_marginal}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_joint_fdist_marginal}
  \rocqok
  \uses{def:guess_joint_fdist,def:guess_sample_fdist}
  % proof: guess_joint_fdist = fdistmap (projection to (guess, V2)) of the sample
  %   fdist, by sdistr_to_fdist mass preservation.
  The joint fdist (Definition~\ref{def:guess_joint_fdist}) is the
  $(\guess,V_2)$-marginal of the sample distribution
  (Definition~\ref{def:guess_sample_fdist}).
  \[ \jointfdist = \mathrm{margin}_{(\guess,V_2)}\,P \]
\end{lemma}

\section{The guess kernel and conditional independence}

\begin{lemma}[The output-determined guess kernel]
  \label{lem:guess_triple_pr}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_inner_kernel_form,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_inner_out,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.view_marginal_indep,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_triple_pr}
  \rocqok
  \uses{def:guess_sample_fdist,lem:Pr_fst_agree_locs}
  % proof: peel the experiment run to a guess kernel (guess_inner_kernel_form);
  %   the guess marginal reaches the secrets only through their scalar-product
  %   output (guess_inner_out, view_marginal_indep); collapse the two uniform
  %   secret samples (dlet_uniform + the msg_of_idx bijection).
  The joint mass of $(\guess,V_2,V_3)$ is the uniform secret-pair weight times an
  output-determined guess kernel $K$: the secrets are uniform on the plaintext
  square, and the guess law given the secrets reaches them only through $S$.
  \[ \Pr[\,(\guess,V_2,V_3)=(x,y,v_3)\,]
        = m^{-2}\cdot K\big(\mathrm{Sout}(y,v_3)\big)(x) \]
\end{lemma}

\begin{lemma}[The predictor reads only the view and $S$]
  \label{lem:run_heap_agree_predictor}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.run_heap_agree_predictor}
  \rocqok
  \uses{lem:Pr_fst_agree_locs}
  % proof: every heap in the support of the capturing run agrees with the start
  %   heap off the two protocol cells; the predictor's locations exclude the V2
  %   cell, so Pr_fst_agree_locs gives an equal guess distribution.
  Every heap the experiment run produces agrees, on the predictor's locations,
  with the start heap; since those locations exclude the $V_2$ cell, the
  predictor's guess distribution is a function of the view and $S$ alone.
\end{lemma}

\begin{lemma}[Guess is conditionally independent of $V_2$ given $S$]
  \label{lem:guess_cinde_V2}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_cinde_V2}
  \rocqok
  \uses{lem:guess_triple_pr,lem:run_heap_agree_predictor,lem:guess_joint_fdist_marginal}
  % proof: cinde_RV_factor with f the V3-fibre weight and g the kernel mass;
  %   pfwd1[guess,V2,Sout] is the V3-fibre sum of the triple law (guess_triple_pr).
  Over the sample distribution the guess is conditionally independent of the
  challenge secret $V_2$ given the leaked output $S$.
  \[ P \models \guess \perp V_2 \mid S \]
\end{lemma}

\section{Instantiating the fiber bound}

\begin{lemma}[Per-fiber bound on $V_2$]
  \label{lem:guess_V2_cond_le}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_VarRV_uniform,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_inputs_indep,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_S_determined,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_VarRV_cond_uniform,
       infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_V2_cond_le}
  \rocqok
  \uses{lem:exist_pr_sol,def:guess_sample_fdist}
  % proof: instantiate Pr_dsdp_sol_uniform_ring at P with the marginal facts
  %   (guess_VarRV_uniform: (V2,V3) uniform; guess_inputs_indep; guess_S_determined);
  %   marginalize V3 out (guess_VarRV_cond_uniform) and lift the conditioning from
  %   the constraint to Sout alone (guess_V2_cond_Sout).
  Conditioned on the leaked output $S$, the secret $V_2$ takes any fixed value
  with probability at most $1/m$. (Over the sample distribution $P$, $(V_2,V_3)$
  is uniform and independent of the inputs, and $S$ is their constraint function;
  these are the hypotheses the route-F fiber bound consumes.)
  \[ \Pr[\,V_2 = a \mid S = s\,] \le 1/m \]
\end{lemma}

\begin{lemma}[Conditional-independence diagonal bound]
  \label{lem:cinde_diagonal_bound}
  \rocq{infotheo.dumas2017dual.lib.extra_proba.cinde_diagonal_bound}
  \rocqok
  When $X$ and $Y$ are conditionally independent given $Z$ and each value of $Y$
  has conditional probability at most $1/m$ given $Z$, the probability of the
  diagonal $X = Y$ is at most $1/m$.
  \[ X \perp Y \mid Z,\quad \Pr[\,Y = a \mid Z = c\,] \le 1/m
        \;\Longrightarrow\; \Pr[\,X = Y\,] \le 1/m \]
\end{lemma}

\begin{lemma}[Fiber residual]
  \label{lem:guess_fdist_success_le}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_fdist_success_le}
  \rocqok
  \uses{lem:cinde_diagonal_bound,lem:guess_cinde_V2,lem:guess_V2_cond_le,lem:guess_joint_fdist_marginal}
  % proof: guess_fdist_success := Pr_[guess_joint_fdist][diag]; rewrite onto P by
  %   the marginal, then cinde_diagonal_bound(guess_cinde_V2, guess_V2_cond_le).
  Over the joint fdist, the predictor guesses $V_2$ with probability at most
  $1/m$: the guess is conditionally independent of $V_2$ given $S$, and given $S$
  the secret takes any value with probability at most $1/m$.
  \[ \Pr\nolimits_{\jointfdist}[\,\guess=V_2\,] \le 1/m \]
\end{lemma}

\section{The output bound and composition}

\begin{theorem}[Information-theoretic bound at the endpoint]
  \label{thm:guess_sdistr_success_le}
  \rocq{infotheo.dumas2017dual.dsdp.dsdp_main.dsdp_alice_guess_ideal_le}
  \rocqok
  \uses{lem:guess_success_sdistr_eq_fdist,lem:guess_fdist_success_le}
  % proof: rewrite Prexp(zero_S) by the connector guess_success_sdistr_eq_fdist,
  %   then guess_fdist_success_le.
  When the third weight $u_3$ acts injectively (so the fiber has $m$ points), at
  the output-exposing all-zero endpoint the predictor guesses $V_2$ with
  probability at most $1/m$. The bound also carries the predictor's losslessness
  and the cardinality and marshalling side-conditions of the guessing layer.
  \[ u_3 \text{ injective} \;\Longrightarrow\; \Prexp(\zero_S) \le 1/m \]
\end{theorem}

\begin{lemma}[Computational distance between the experiments]
  \label{lem:guess_advantage_le}
  \rocq{infotheo.dumas2017dual.dsdp.indcpa_hopping.dsdp_guess_fiber.guess_advantage_eq,
       infotheo.dumas2017dual.dsdp.dsdp_main.dsdp_alice_guess_advantage_le}
  \rocqok
  \uses{def:guessing_experiment_leak_S,lem:dsdp_advantage_derived_leak_S,def:relational_advantage}
  % proof: guess_advantage_eq slides the fixed predictor out of the par
  %   (Advantage_par); guess_advantage_le then applies dsdp_advantage_derived_leak_S
  %   (eapply, since apply: delta-unfolds the linked games).
  The success-probability difference on the real and all-zero games equals the
  advantage of the challenger-wrapped predictor, a valid adversary, and that
  advantage is at most $2\,\epscpa$ by the Part~II computational distance
  (Lemma~\ref{lem:dsdp_advantage_derived_leak_S}). The equality is one lemma and
  the bound another.
  \[ \lvert\,\Prexp(\real_S) - \Prexp(\zero_S)\,\rvert
        = \AdvE(\real_S, \zero_S, \text{pred}) \le 2\,\epscpa \]
\end{lemma}

\begin{theorem}[Full secrecy by composition]
  \label{thm:dsdp_alice_secrecy_leak_S}
  \rocq{infotheo.dumas2017dual.dsdp.dsdp_main.dsdp_alice_guess_real_le}
  \rocqok
  \uses{thm:guess_sdistr_success_le,lem:guess_advantage_le}
  % proof: triangle Prexp(real_S) <= Prexp(zero_S) + |Prexp(real_S) - Prexp(zero_S)|,
  %   then the endpoint bound (1/m) and the computational distance (2*epsilon_cpa).
  When $u_3$ acts injectively, and for a predictor whose locations are disjoint
  from the two encryption oracles' state (the marshalling round-trips cancelling),
  the predictor guesses $V_2$ against the output-exposing real game with
  probability at most $1/m + 2\,\epscpa$.
  \[ u_3 \text{ injective} \;\Longrightarrow\; \Prexp(\real_S) \le 1/m + 2\,\epscpa \]
\end{theorem}
