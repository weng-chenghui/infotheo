# DSDP headline assumptions allowlist

Date: 2026-07-29. Branch `20260729-0028-reduction-form-security`, after phase 8
(`ec69db19`). Generated by `Print Assumptions` on every `dsdp_main.v` headline,
compiled against the built tree (`coqc -R . infotheo`, local switch
`~/Projects/coq/_opam`). Raw output reproduced below; this note records the
committed result required by the spec's Verification section
(`20260728-reduction-form-security-statements.md`).

**No project declaration appears in any headline's trust base.** In particular
`epsilon_cpa` and `enc_ind_cpa_real_or_zero` are deleted from the tree, and no
headline references any replacement assumption. `absord` / `unlock_absord`
(anticipated in the spec's expected list) do not occur either.

## Per-headline assumption sets

Information-theoretic headlines — exactly the mathcomp-analysis boolp trio:

- `dsdp_centropy_uniform`, `dsdp_centropy_uniform_n`,
  `US_n_compromised_leaks_secret`, `US_compromised_leaks_V2`,
  `bob_privacy_V1`, `charlie_privacy_V1`, `bob_privacy_V3`,
  `charlie_privacy_V2`:
  - `boolp.propositional_extensionality`
  - `boolp.functional_extensionality_dep`
  - `boolp.constructive_indefinite_description`

SSProve-leg headlines — the trio plus the SSProve/mathcomp upstream baseline:

- `dsdp_alice_view_advantage_le`, `dsdp_alice_guess_V2_zero_le`,
  `dsdp_alice_guess_advantage_le`, `dsdp_alice_guess_V2_real_le`,
  `dsdp_alice_unpredictability_entropy_ge`,
  `dsdp_alice_simulation_advantage_le`:
  - the boolp trio above
  - `FunctionalExtensionality.functional_extensionality_dep` (Stdlib)
  - `SPropBase.ax_proof_irrel : ClassicalFacts.proof_irrelevance` (SSProve)
  - `Axioms.R : reals.Real.type` (SSProve's abstract realType)
  - `realsum.__admitted__interchange_psum` (mathcomp experimental_reals;
    an `Admitted` Fubini-style psum interchange used at `distr.v:516` —
    pre-existing, recorded as a follow-up in the spec)

## Raw output

The verbatim `coqc` output (Locate header + Print Assumptions per headline) is
archived in git at this commit; regenerate with a scratch file of
`Locate <thm>. Print Assumptions <thm>.` pairs `Require`-ing
`infotheo.dumas2017dual.dsdp.dsdp_main`.
